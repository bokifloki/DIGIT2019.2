import React from 'react';
import styled from 'styled-components';

import styles from "./Homepage.css"

const Homepage = () => {
    return (
        <div className={styles.mace}>pozdrav</div>
    );
}

export default Homepage;